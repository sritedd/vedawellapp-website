# VedaWell Next — Required Environment Variables
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# In production (Netlify), these are set automatically by the
# Netlify Supabase extension when you select "Next.js" as the framework.

# Supabase (get from https://supabase.com → Project Settings → API)
NEXT_PUBLIC_SUPABASE_DATABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: used by Netlify Supabase extension (not needed in app code)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# SUPABASE_JWT_SECRET=your-jwt-secret-here
